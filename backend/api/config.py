# backend/api/config.py

class Config:
    # Basic configuration for API
    API_HOST = '127.0.0.1'  # Listen only on localhost for security
    API_PORT = 8000        # Port to run the API on

    # Any other configuration you want to add
    # For example, you could add API keys, database config, etc.
